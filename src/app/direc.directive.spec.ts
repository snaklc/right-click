import { DirecDirective } from './direc.directive';

describe('DirecDirective', () => {
  it('should create an instance', () => {
    const directive = new DirecDirective();
    expect(directive).toBeTruthy();
  });
});
